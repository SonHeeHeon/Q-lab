/// File: app/lib/domain/entities/principle.dart
///
/// Role:
///   Dart mirror of `shared.domain.principle.Principle` plus the
///   `PrincipleCategory` enum (ABSOLUTE | CRITERIA | FREE_NOTE).
///
/// UI note:
///   isEditable=false → UI disables edit/delete controls and shows
///   a tooltip explaining the intentional friction.
///
/// Connected modules:
///   - data/api/principles_api.dart
///   - presentation/principles/ (CRUD)
///   - presentation/trade_journal/ (multi-select)
